<Window x:Class="Guardian.App.PasswordPromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="家长密码"
        Width="380"
        Height="220"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F7F8FA">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="请输入家长密码"
                   FontSize="20"
                   FontWeight="SemiBold"
                   Foreground="#20242A" />
        <TextBlock x:Name="ReasonText"
                   Grid.Row="1"
                   Margin="0,8,0,16"
                   TextWrapping="Wrap"
                   Foreground="#606873" />
        <PasswordBox x:Name="PasswordInput"
                     Grid.Row="2"
                     Height="34"
                     VerticalContentAlignment="Center" />
        <TextBlock x:Name="ErrorText"
                   Grid.Row="3"
                   Margin="0,8,0,0"
                   Foreground="#B42318"
                   TextWrapping="Wrap" />

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="取消"
                    Width="78"
                    Margin="0,0,10,0"
                    Click="Cancel_Click" />
            <Button Content="确认"
                    Width="88"
                    IsDefault="True"
                    Click="Confirm_Click" />
        </StackPanel>
    </Grid>
</Window>
