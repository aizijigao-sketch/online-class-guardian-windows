<Window x:Class="Guardian.App.SetPasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置家长密码"
        Width="420"
        Height="300"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F7F8FA">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="首次使用：设置家长密码"
                   FontSize="21"
                   FontWeight="SemiBold"
                   Foreground="#20242A" />
        <TextBlock Grid.Row="1"
                   Margin="0,8,0,18"
                   Text="关闭网课模式、退出程序时需要这个密码。密码只会以加盐哈希保存。"
                   TextWrapping="Wrap"
                   Foreground="#606873" />

        <TextBlock Grid.Row="2"
                   Text="输入密码"
                   Foreground="#333942" />
        <PasswordBox x:Name="PasswordInput"
                     Grid.Row="3"
                     Height="34"
                     Margin="0,6,0,12"
                     VerticalContentAlignment="Center" />

        <TextBlock Grid.Row="4"
                   Text="再次确认"
                   Foreground="#333942" />
        <PasswordBox x:Name="ConfirmInput"
                     Grid.Row="5"
                     Height="34"
                     Margin="0,6,0,0"
                     VerticalAlignment="Top"
                     VerticalContentAlignment="Center" />
        <TextBlock x:Name="ErrorText"
                   Grid.Row="5"
                   Margin="0,48,0,0"
                   Foreground="#B42318"
                   TextWrapping="Wrap" />

        <StackPanel Grid.Row="6"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="退出"
                    Width="78"
                    Margin="0,0,10,0"
                    Click="Cancel_Click" />
            <Button Content="保存密码"
                    Width="96"
                    IsDefault="True"
                    Click="Save_Click" />
        </StackPanel>
    </Grid>
</Window>
